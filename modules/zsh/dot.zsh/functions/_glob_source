function _glob_source()
{
    while [ -n "$1" ]; do
        _glob_args $1 _source_if_exists
        shift
    done
}

function _source_if_exists()
{
    if [ -f "$1" ]; then
        source "$1"
    fi
}
